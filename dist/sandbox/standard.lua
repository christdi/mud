return {
  table = table,
  ipairs = ipairs,
  dump = dump,
  one_argument = one_argument,
  join = join,
  log = log,
  game = game,
  player = player,
  actions = actions,
  events = events,
  commands = commands,
  players = players,
  observable_archetype = observable_archetype,
  goable_archetype = goable_archetype,
  lunac = lunac
}