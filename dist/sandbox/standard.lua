return {
  table = table,
  ipairs = ipairs,
  pairs = pairs,
  tostring = tostring,
  dump = dump,  
  one_argument = one_argument,
  join = join,
  log = log,
  game = game,
  player = player,
  actions = actions,
  events = events,
  commands = commands,
  players = players,
  script = script,
  lunac = lunac
}